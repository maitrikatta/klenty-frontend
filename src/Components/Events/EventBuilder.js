import { Paper } from "@mui/material";

function EventBuilder() {
  return (
    <Paper
      sx={{
        height: "100px",
      }}
      square={true}
      elevation={8}
    >
      EventBuilder
    </Paper>
  );
}

export default EventBuilder;
